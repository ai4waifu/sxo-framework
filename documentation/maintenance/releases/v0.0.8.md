# 🚀 v0.0.8

## ✨ Features

- **MATLAB control-flow closure**: `While` conditions evaluate each iteration; loop bodies support `Max`/`Min`/`Mod` and scalar assignment.
- **MATLAB leetcode anchors**: `two-sum`, `reverse-integer`, and `container-with-most-water` HostSession conformance green with coach-aligned `while`/`for` solutions.
- **Script builtins**: `intmin('int32')` / `intmax('int32')` and integer `floor(n/m)` lowering via `Quotient` on the script path.

## 🐛 Bug Fixes

- MATLAB `While[l < r, …]` no longer spins when the condition used `Unequal` / `Less` on rebound locals.
- Wolfram `IntegerDigits` palindrome path remains stable for harness-sized integers (see S-018 for metadata mega-float cases).

## ⚠️ Breaking Changes

None

## 👥 Contributors

<table>
<tbody>
<tr>
<td align="center"><a href="https://github.com/oovm"><img src="https://avatars.githubusercontent.com/u/17541209?s=100" width="64" height="64" alt="@oovm"/><br /><sub><b>oovm</b></sub></a></td>
</tr>
</tbody>
</table>

## 📝 Other

- `floor` inside MATLAB user `function` bodies remains open (use integer `(n - rem) / 10` peel in leetcode solvers until a later slice).
