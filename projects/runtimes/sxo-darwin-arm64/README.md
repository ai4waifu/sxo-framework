# Native Platform Artifact: macOS arm64

This package contains the macOS arm64 native SXO addon selected by supported high-level packages. Do not install or import it as a general API. Install the package that represents your workflow and let npm resolve the optional platform dependency.

Loading requires a matching macOS arm64 host and Node.js ABI. Check architecture, optional dependency installation, and clean deployment behavior when troubleshooting. Apache License 2.0. See [LICENSE](../../../../LICENSE).
